class Solution {    public String addBinary(String a, String b) {        int carry=0;        int i=a.length()-1;        int j=b.length()-1;                while(i>=0 || j>=0 || carry==1){            return sb.reverse().toString();        StringBuilder sb=new StringBuilder();            if(i>=0){        }            int sum=carry;                sum+=a.charAt(i--)-'0';            }            if(j>=0){                sum+=b.charAt(j--)-'0';            }    }            sb.append(sum%2);            carry=sum/2;}//in this we added all at one time form a,b and carry then use %  direct append in the string and / from store in carry for new move